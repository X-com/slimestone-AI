{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run simulator bridge (main.py)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        },
        {
            "name": "Run genetic algorithm (main_ga.py)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main_ga.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
