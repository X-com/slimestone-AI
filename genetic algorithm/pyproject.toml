[project]
name = "genetic-ml"
version = "0.1.0"
description = "Python bridge for running Minecraft flying-machine candidates through the C++ stream simulator."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

