[project]
name = "transformer-gym"
version = "0.1.0"
description = "Graph-transformer that predicts flying-machine physics from the C++ simulator's simulation_data log."
requires-python = ">=3.11"
dependencies = ["torch>=2.9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
